/**
 * CONFIG
 * Define formato do estado global do aplicativo.
 */
export type AppStateConfigTP = {
    authToken: string,
    userName: string,
    currentAddress: string,
    latitude: number,
    longitude: number,
}
